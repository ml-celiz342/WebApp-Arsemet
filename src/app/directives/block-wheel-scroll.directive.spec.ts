import { BlockWheelScrollDirective } from './block-wheel-scroll.directive';

describe('BlockWheelScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockWheelScrollDirective();
    expect(directive).toBeTruthy();
  });
});
